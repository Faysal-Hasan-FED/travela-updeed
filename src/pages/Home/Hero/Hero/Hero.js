import React from 'react';
import HeroText from '../HeroText/HeroText';
import './Hero.css';

const Hero = () => {
    return (
        <div className='home-hero'>
        <HeroText></HeroText>
        </div>
    );
};

export default Hero;